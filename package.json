{
  "name": "ai-rmf-oscal-catalog",
  "version": "0.4.0",
  "description": "Tooling deps for OSCAL schema validation of AI RMF catalog and worked-example profiles",
  "private": true,
  "license": "CC0-1.0",
  "scripts": {
    "validate-catalog": "ajv validate -s schemas/oscal_catalog_schema.json -d catalogs/ai-rmf-v0.4.json -c ajv-formats --strict=false --spec=draft7",
    "validate-profile-example-1": "ajv validate -s schemas/oscal_profile_schema.json -d profiles/ai-rmf-example-1-profile.json -c ajv-formats --strict=false --spec=draft7",
    "validate-profile-example-2": "ajv validate -s schemas/oscal_profile_schema.json -d profiles/ai-rmf-example-2-profile.json -c ajv-formats --strict=false --spec=draft7",
    "validate-profiles": "npm run validate-profile-example-1 && npm run validate-profile-example-2",
    "validate": "npm run validate-catalog && npm run validate-profiles"
  },
  "devDependencies": {
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1"
  }
}
